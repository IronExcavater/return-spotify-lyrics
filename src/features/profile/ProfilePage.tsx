export function ProfilePage() {
    return (
        <section className="p-5">
            <h1 className="text-2xl font-semibold">Profile</h1>
            <p className="mt-1 text-sm text-text-muted">
                Account data will come from Spotify and Firebase queries.
            </p>
        </section>
    );
}
