export function QueuePage() {
    return (
        <section className="p-5">
            <h1 className="text-2xl font-semibold">Queue</h1>
            <p className="mt-1 text-sm text-text-muted">
                Spotify queue data will be added as a Query-backed feature.
            </p>
        </section>
    );
}
